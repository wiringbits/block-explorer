import { ExplorerAmountPipe } from './explorer-amount.pipe';

describe('ExplorerAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new ExplorerAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
